#include<stdio.h>
int main ()
{
    int length,breadth,area,perimeter;
    printf("Enter the length and breadth :");
    scanf("%d%d",&length,&breadth);
    area= length*breadth ;
    printf("%d",area);
    return 0;
}